div(class="game-panel-container")
    label(for="answer")
    input(class="input-answer" id="input-answer", type="text")
    div(class="profile")
        div(class="logo-div")
            img(class="game-panel__user-logo",
            src=userData.avatar_url,
            alt="User")
        div(class="panel-text") #{userData.username}            
    div(id="changing-element" class="control")
        input(class="game-panel-button-without-hover" value="Выберите вопрос", id="changing-button" type="submit")
    div(class="score")
        div
            p(class="panel-text") Счет: 
            p(class="panel-text" id="score") 0