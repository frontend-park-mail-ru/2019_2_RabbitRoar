div(class="text-container")
    div(class="text") Для создания пака выберите ячейку с темой и вопросом! 

mixin cell(_id, theme)
    - const cost = (_id + 1) * 100;
    - const modifyId = "" + theme + _id;
    div(class="question-container__cost" id=modifyId) #{cost}

mixin line(theme_id, theme_name)
    div(class="question-container__line" id=theme_id)
        div(class="question-container__theme-hover") #{theme_name}
        +cell(0, theme_name)
        +cell(1, theme_name)   
        +cell(2, theme_name)
        +cell(3, theme_name)
        +cell(4, theme_name)

div(class="question-container")
    each theme, index in ['Тема 1', 'Тема 2', 'Тема 3', 'Тема 4', 'Тема 5']
        +line(theme, theme)
form(class="form")
    button(type="submit", id="save-pack") Сохранить пак
    button(type="submit", id="back") Назад

div(class="popup" id="popup")
    div(class="popup__content " id="popup_content")
        div(class="popup-elem-top" id="popup-elem-top") Дефолтный текст
        div(class="popup-elem-top")
            textarea(class="input-textarea" maxlength="150")
        div(class="popup-elem-bottom ")
            div(class="popup-button" id="cansel") Отмена
            div(class="popup-button" id="save") Сохранить