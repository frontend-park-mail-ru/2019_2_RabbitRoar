div(class="popup " id="popup")
    div(class="popup__content " id="popup_content")
        div(class="popup-elem-top" id="popup-elem-top") Невозможно подключиться к комнате
        div(class="popup-elem-bottom ")
            div(class="popup-button" id="continue") На главную

if roomState === "done_connection"
    div(class="panel-text") #{userData.username}, ожидайте других игроков!
    div(class="user-panel-container")
        div(id="main-user")
            div(class="player-avatar")
                div(class="logo-div")
                    img(class="game-panel__user-logo",
                    src=userData.avatar_url,
                    alt="User")
                div(class="panel-text") #{userData.username}
            div(class="users-panel__ready-indicator users-panel__ready-indicator__true", id="capitan-ready")
        div(class="other-user")
            div(class="player-avatar")
            div(class="users-panel__ready-indicator users-panel__ready-indicator__true", id="ready")
        div(class="other-user")
            div(class="player-avatar")
            div(class="users-panel__ready-indicator users-panel__ready-indicator__false", id="ready")
        div(class="other-user")
            div(class="player-avatar")
            div(class="users-panel__ready-indicator users-panel__ready-indicator__false", id="ready")
    div(class="button-ready-button") Готов

else if roomState === "waiting"

else if roomState === "game"